version: "2"
linters:
  default: none
  enable:
    - gosec
    - govet
    - errcheck
    - noctx
    - bodyclose
    - staticcheck
    - ineffassign
    - misspell

formatters:
  enable:
    - gofmt

issues:
  max-issues-per-linter: 0
  max-same-issues: 0

run:
  timeout: 5m
  tests: true
